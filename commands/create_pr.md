<!-- Create pull request -->
Create a PR using gh with these changes. Make sure to:
- fetch fresh main from origin
- create feature branch off main if not created yet
- commit changes if not committed yet
- push and create PR to main
- be concise in PR description, don't include "created with claude"
